# Environment Configuration Example
# Скопируйте этот файл в .env и настройте под ваше окружение

# Node Environment
NODE_ENV=production

# Frontend Port (по умолчанию 80 в docker)
FRONTEND_PORT=80

# Backend API URL (используется только для локальной разработки)
# В production (Docker) запросы идут через nginx proxy
# Для локальной разработки оставьте пустым - будет использоваться Vite proxy
VITE_API_BASE_URL=
