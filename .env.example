# Environment Configuration Example
# Скопируйте этот файл в .env и настройте под ваше окружение

# Node Environment
NODE_ENV=production

# Frontend Port (по умолчанию 80 в docker)
FRONTEND_PORT=80

# Backend URL (если нужно указать в коде напрямую, обычно не требуется)
# VITE_BACKEND_URL=http://your-backend-url:8080
